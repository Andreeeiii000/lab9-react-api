Вот готовый **README.md**, полностью под твою лабораторную работу №9 (**Каталог фильмов**, React + API).
Ты можешь просто вставить его в свой проект и заменить скриншоты на свои.

---

# 📌 **Лабораторная работа №9**

### *Основы веб-разработки с применением ИИ-ассистентов*

### Мини-проект: **Каталог фильмов (Movie Search)**

API: [https://www.omdbapi.com/](https://www.omdbapi.com/)

---

## 📌 **Название выбранного мини-проекта**

**Вариант A — Каталог фильмов (Movie Search)**
Приложение позволяет искать фильмы по названию через внешний API OMDb и просматривать подробную информацию о каждом фильме.

---

## 📌 **Пример использованного API-запроса**

### 🔍 Поиск фильмов:

```
https://www.omdbapi.com/?apikey=589e8037&s=Matrix
```

### 📄 Подробная информация о фильме:

```
https://www.omdbapi.com/?apikey=589e8037&i=tt0133093&plot=full
```

> В приложении параметр `apikey` хранится в константе `API_KEY` и подставляется автоматически.

---

## 📸 **Скриншоты работы приложения**

(замени на свои)

```
/screenshots/search.png
/screenshots/movie-list.png
/screenshots/movie-modal.png
```

---

## 📘 **Ответы на вопросы**

---

### ❓ **1. Что делает useEffect в вашем приложении?**

В приложении используется два хука `useEffect`:

1. **Первый useEffect** — вызывается при изменении текста поиска (`searchTerm`).
   Он отправляет запрос к API OMDb на поиск фильмов:

```js
useEffect(() => {
  if (!searchTerm) return;
  // запрос на список фильмов
}, [searchTerm]);
```

2. **Второй useEffect** — срабатывает при выборе конкретного фильма (`selectedMovieId`).
   Он загружает подробную информацию о фильме и показывает её в модальном окне:

```js
useEffect(() => {
  if (!selectedMovieId) return;
  // запрос деталей фильма
}, [selectedMovieId]);
```

Таким образом, **useEffect отвечает за выполнение побочных эффектов — загрузку данных по API.**

---

### ❓ **2. Какие состояния вы использовали и зачем?**

В приложении используются следующие состояния (`useState`):

| Состояние         | Для чего нужно                                       |
| ----------------- | ---------------------------------------------------- |
| `searchTerm`      | Текст, который пользователь вводит в поиске          |
| `movies`          | Массив найденных фильмов                             |
| `selectedMovieId` | ID фильма, по которому открывается модалка           |
| `selectedMovie`   | Полная информация о выбранном фильме                 |
| `loading`         | Показ индикатора загрузки списка фильмов             |
| `modalLoading`    | Показ индикатора загрузки деталей фильма             |
| `error`           | Отображение ошибок API (например: "Фильм не найден") |

Эти состояния позволяют приложению реагировать на действия пользователя и корректно отображать результаты поиска.

---

### ❓ **3. Где ИИ помог, а где пришлось разбираться самому?**

**ИИ помог:**

* сгенерировать каркас проекта и структуру компонентов (`SearchBar`, `MovieList`, `MovieCard`, `MovieModal`);
* написать корректные запросы к OMDb API;
* объяснить зависимости в `useEffect` и логику разделения состояний;
* оптимизировать обработку ошибок и индикатор загрузки;
* оформить стили и сетку карточек.

**Пришлось разбираться самому:**

* создавать реальный API-ключ на сайте OMDb;
* исправлять ошибку `Invalid API key` и проверять запросы в Network;
* разобраться с правильным путём запуска React-проекта и устранить ошибки npm;
* вручную переносить компоненты и устранять конфликты файлов.

---

### ❓ **4. Что из документации API было важнее всего?**

Самые важные моменты из документации OMDb API:

1. **Параметры запроса:**

   * `s=title` — поиск по названию;
   * `i=id` — получение деталей по IMDb ID;
   * `plot=full` — полное описание фильма;
   * `apikey=XXXX` — уникальный ключ (без него API не работает).

2. **Ограничения:**

   * ключ `demo` не работает;
   * при ошибке сервер всегда возвращает `"Response": "False"` и текст `"Error"`.

3. **Структура ответа:**

   * список фильмов приходит в `Search: [...]`;
   * полные данные содержат `Genre`, `Actors`, `Plot`, `imdbRating` и др.

Эта информация позволила правильно настроить приложение и реализовать все требования.

---

## ✔ Итог

Приложение успешно загружает данные через API, использует useEffect, обрабатывает ошибки, отображает загрузку и показывает фильмы в удобном интерфейсе. В разработке активно применялись подсказки ИИ-ассистента.

---

Если хочешь — могу добавить в README.md **ещё раздел: “Как запустить проект (npm install / npm start)”** или сделать версию в PDF.
